import 'package:flutter_test/flutter_test.dart';

import 'package:iteman/main.dart';

void main() {
  test('Iteman app widget can be constructed', () {
    expect(const ItemanApp(), isA<ItemanApp>());
  });
}
